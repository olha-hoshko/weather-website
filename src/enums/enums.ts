export enum ThemeChangeButtonIcons {
  light = 'light',
  dark = 'dark',
};

export enum AirCondition {
  real_feel = 'real-feel',
  chance_of_rain = 'chance-of-rain',
  wind = 'wind',
  uv_index = 'uv-index',
}